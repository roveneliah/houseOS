import { dao } from "../../config";

describe("Can find proposal", () => {
  it("via search", () => {});
  it("via list", () => {});
});

describe("When looking at proposal", () => {
  it("can view top labels", () => {});
  it("can view status", () => {});
  it("can view vote for each outcome", () => {});
  it("can view comments", () => {});
});
