import { useOrderedMultiselect } from "./useOrderedMultiselect";

describe("useOrderedMultiselect", () => {
  it.todo("all elements should be unselected on initialization");
  it.todo("selecting unselected item should move it to head of array");
  it.todo(
    "unselected unselected item should move it to first unselected element"
  );

  it.todo("selected items should always be first in array");
  it.todo("calling an item's toggle should flip its selected boolean");
});
